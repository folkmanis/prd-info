import { DrawerSmallDirective } from './drawer-small.directive';

describe('DrawerSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerSmallDirective();
    expect(directive).toBeTruthy();
  });
});

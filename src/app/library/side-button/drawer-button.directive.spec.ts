import { DrawerButtonDirective } from './drawer-button.directive';

describe('DrawerButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerButtonDirective();
    expect(directive).toBeTruthy();
  });
});

import { DenseListDirective } from './dense-list.directive';

describe('DenseListDirective', () => {
  it('should create an instance', () => {
    const directive = new DenseListDirective();
    expect(directive).toBeTruthy();
  });
});

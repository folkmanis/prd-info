import { IfViewSizeDirective } from './if-view-size.directive';

describe('IfViewSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new IfViewSizeDirective();
    expect(directive).toBeTruthy();
  });
});

import { IsLargeDirective } from './is-large.directive';

describe('IsLargeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLargeDirective();
    expect(directive).toBeTruthy();
  });
});

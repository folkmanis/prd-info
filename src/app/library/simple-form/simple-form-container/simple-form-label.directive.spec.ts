import { SimpleFormLabelDirective } from './simple-form-label.directive';

describe('SimpleFormLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleFormLabelDirective();
    expect(directive).toBeTruthy();
  });
});

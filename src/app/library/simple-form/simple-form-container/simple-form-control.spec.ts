import { SimpleFormControl } from './simple-form-control';

describe('SimpleFormControl', () => {
  it('should create an instance', () => {
    expect(new SimpleFormControl()).toBeTruthy();
  });
});

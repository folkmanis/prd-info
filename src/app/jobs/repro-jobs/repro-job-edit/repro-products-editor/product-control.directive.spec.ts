import { ProductControlDirective } from './product-control.directive';

describe('ProductControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductControlDirective();
    expect(directive).toBeTruthy();
  });
});

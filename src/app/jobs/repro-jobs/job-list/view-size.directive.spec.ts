import { ViewSizeDirective } from './view-size.directive';

describe('ViewSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewSizeDirective();
    expect(directive).toBeTruthy();
  });
});

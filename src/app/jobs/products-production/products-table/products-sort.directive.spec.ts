import { ProductsSortDirective } from './products-sort.directive';

describe('ProductsSortDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductsSortDirective();
    expect(directive).toBeTruthy();
  });
});

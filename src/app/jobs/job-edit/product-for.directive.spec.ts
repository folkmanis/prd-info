import { ProductForOfDirective } from './product-for.directive';

describe('ProductForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductForOfDirective();
    expect(directive).toBeTruthy();
  });
});

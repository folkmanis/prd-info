import { ThreadsPaginatorDirective } from './threads-paginator.directive';

describe('ThreadsPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreadsPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});

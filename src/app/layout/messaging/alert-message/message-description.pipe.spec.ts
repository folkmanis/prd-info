import { MessageDescriptionPipe } from './message-description.pipe';

describe('MessageDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});

import { MessageActionsPipe } from './message-actions.pipe';

describe('MessageActionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageActionsPipe();
    expect(pipe).toBeTruthy();
  });
});

import { MessageJobDirective } from './message-job.directive';

describe('MessageJobDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageJobDirective();
    expect(directive).toBeTruthy();
  });
});

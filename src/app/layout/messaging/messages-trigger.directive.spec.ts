import { MessagesTriggerDirective } from './messages-trigger.directive';

describe('MessagesTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MessagesTriggerDirective();
    expect(directive).toBeTruthy();
  });
});

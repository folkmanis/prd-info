import { CustomersFormSource } from './customers-form-source';

describe('CustomersFormSource', () => {
  it('should create an instance', () => {
    expect(new CustomersFormSource()).toBeTruthy();
  });
});

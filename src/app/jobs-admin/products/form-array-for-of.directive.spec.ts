import { FormArrayForOfDirective } from './form-array-for-of.directive';

describe('FormArrayForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new FormArrayForOfDirective();
    expect(directive).toBeTruthy();
  });
});

import { MaterialUnitsDirective } from './material-units.directive';

describe('MaterialUnitsDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialUnitsDirective();
    expect(directive).toBeTruthy();
  });
});

import { ProductsFormSource } from './products-form-source';

describe('ProductsFormSource', () => {
  it('should create an instance', () => {
    expect(new ProductsFormSource()).toBeTruthy();
  });
});

import { ProductionStagesFormSource } from './production-stages-form-source';

describe('ProductionStagesFormSource', () => {
  it('should create an instance', () => {
    expect(new ProductionStagesFormSource()).toBeTruthy();
  });
});

import { SystemPreferencesApi } from './system-preferences-api';

describe('SystemPreferencesApi', () => {
  it('should create an instance', () => {
    expect(new SystemPreferencesApi()).toBeTruthy();
  });
});

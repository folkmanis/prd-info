import { ProductionStagesApi } from './production-stages-api';

describe('ProductionStagesApi', () => {
  it('should create an instance', () => {
    expect(new ProductionStagesApi()).toBeTruthy();
  });
});

import { InvoiceDeleteDirective } from './invoice-delete.directive';

describe('InvoiceDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new InvoiceDeleteDirective();
    expect(directive).toBeTruthy();
  });
});

import { TotalValidatorDirective } from './total-validator.directive';

describe('TotalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TotalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

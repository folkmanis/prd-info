import { ActiveVeikalsDirective } from './active-veikals.directive';

describe('ActiveVeikalsDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveVeikalsDirective();
    expect(directive).toBeTruthy();
  });
});
